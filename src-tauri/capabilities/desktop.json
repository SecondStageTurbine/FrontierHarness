{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Frontier's own loopback UI may open the native project folder picker, check for and install updates, restart the app, post desktop notifications, and run terminals in the project folder.",
  "windows": [
    "main"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:*"
    ]
  },
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "updater:default",
    "process:default",
    "notification:default",
    "allow-pty-open",
    "allow-pty-write",
    "allow-pty-resize",
    "allow-pty-close",
    "allow-open-with",
    "allow-keep-awake"
  ]
}
