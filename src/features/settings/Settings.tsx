import {Moon,Sun,ShieldCheck} from 'lucide-react';
import {useWorkspace} from '../../app/context';
import {Field} from '../../components/ui';
export default function Settings({section='General'}:{section?:string}){
 const {theme,setTheme,tenant,tenants,switchTenant,advanced,setAdvanced}=useWorkspace();
 if(section==='Security')return <section className="form-panel"><h3><ShieldCheck size={18}/> Security</h3><p>Projects, models, sessions, and artifacts are scoped to the selected workspace. The backend validates ownership on every request.</p><p>Provider credentials are encrypted in local application storage and used only by the model broker. They are never included in the frontend.</p><h3>Local execution</h3><p>Build & test runs project code with your operating-system account permissions. It is intended for trusted local projects; it is not an operating-system sandbox. Propose only generates inspectable changes without writing files or running commands.</p><p>Project file access excludes credentials, linked directories, and paths outside the selected root. Changes stop if a file has been modified outside the run.</p></section>;
 if(section==='Developer')return <section className="form-panel"><h3>Developer</h3><label className="toggle-row"><div><strong>Advanced output</strong><p>Enable detailed execution payloads in artifact views.</p></div><input type="checkbox" role="switch" checked={advanced} onChange={e=>setAdvanced(e.target.checked)}/></label><dl className="metadata"><dt>Runtime</dt><dd>Tauri desktop + Python workflow engine</dd><dt>Live updates</dt><dd>Persisted server events with polling recovery</dd><dt>Persistence</dt><dd>Local SQLite database</dd><dt>Recovery</dt><dd>Interrupted runs retain outputs and are never replayed automatically.</dd></dl></section>;
 return <section className="form-panel"><h3>Appearance</h3><div className="theme-options"><button className={theme==='dark'?'selected':''} onClick={()=>setTheme('dark')}><Moon size={22}/><strong>Dark</strong><span>Focused and understated</span></button><button className={theme==='light'?'selected':''} onClick={()=>setTheme('light')}><Sun size={22}/><strong>Light</strong><span>Clear and open</span></button></div><Field label="Default / current workspace" hint="Your selection is remembered on this device."><select value={tenant?.id||''} onChange={e=>switchTenant(e.target.value)}>{tenants.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></Field><h3>Keyboard shortcuts</h3><dl className="metadata"><dt>Enter</dt><dd>Send an instruction</dd><dt>Shift + Enter</dt><dd>New line</dd><dt>Ctrl + N</dt><dd>New session</dd><dt>Ctrl + K</dt><dd>Find a session</dd><dt>Escape</dt><dd>Close the contextual panel</dd></dl></section>;
}
