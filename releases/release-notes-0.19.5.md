Frontier 0.19.5: pairing works with a VPN on

- The pairing code now uses your local network address (or Tailscale), not a VPN tunnel or virtual adapter address the phone cannot reach. Each address is labelled.
- If a VPN on the computer still blocks the phone, allow LAN connections in the VPN's settings, or pair over Tailscale.
