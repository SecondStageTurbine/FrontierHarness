Frontier 0.20.5

- The setup wizard lists OpenCode's real models, read from OpenCode itself. It used to offer "opencode/free", which OpenCode does not have: if you connected it, change its model identifier in Agents & Providers to one of OpenCode's, such as opencode/nemotron-3-ultra-free.
- Team mode: the lead names the agent for every task and keeps the strongest agents for integration and hard debugging, instead of leaving tasks to the cheapest agent.
