Frontier 0.13.3: subscription limits in Settings → Usage

- Settings → Usage now opens with your Claude and Codex subscription limits, the same figures as the card beside the composer, so any window width can see them.
- The Usage totals (turns, tokens, agent time, cost) are laid out as labelled tiles instead of running together.
