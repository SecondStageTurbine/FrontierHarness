Frontier 0.20.3

- Codex can use the MCP servers you added in Settings → MCP & Skills (it refused their tools before, because nobody can approve them during a Frontier turn). Under Read only, only tools that only read run.
- With a project's agent browser on, it replaces a plain Playwright server in the workspace, so agents use the browser you chose (your own Chrome or Edge, or a fresh one).
