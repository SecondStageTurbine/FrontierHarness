Frontier 0.13.2: subscription usage beside the composer

- A Usage card beside the composer, on wide windows, shows how much of your Claude and Codex subscription windows is used and when each resets, the way each tool's own usage view does. It uses the sign-in each tool already keeps on this computer, so everyone sees their own plans.
